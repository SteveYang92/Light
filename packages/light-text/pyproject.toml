[project]
name = "light-text"
version = "0.1.0"
description = "Shared text utilities — punctuation constants, timecode formatting, CJK detection"
license = "MIT"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
