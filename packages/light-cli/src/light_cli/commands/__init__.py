"""Standalone capability commands for the ``light`` multi-command app.

Each module is a thin wrapper over one capability package
(``light_asr`` / ``light_asr_polish`` / ``light_subtitle``); the pipeline
command itself reuses the legacy ``light-subtitle`` callback unchanged.
"""
