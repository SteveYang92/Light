{{ domain_context }}

Correct ASR transcription errors in the speech segments below.
This transcript was produced by automated speech recognition and may contain
inaccuracies. Fix the following types of mistakes:

  - Homophones and misheard words (e.g. "there" → "their", "treating" → "trading",
    "plate box" → "playbook")
  - Garbled proper nouns or technical terms — match phonetically similar words
    against the known terminology above (e.g. "Ministrol" → "Mistral")
  - Terminology inconsistency — unify spelling of the SAME term within this batch
    so it appears identically across segments
  - Duplicate words (e.g. "A A" → "A") — remove the duplicate, word count -1
  - Split compound words — merge into a single word (word count decreases)
  - Obvious grammar word forms (e.g. "was" → "were", singular/plural)
  - Missing function words — may add or remove up to 2 words when clearly
    justified by context and good English grammar

Rules:
  - For most segments: return the same number of words as the input
  - Word count may differ by at most ±2 from the original; any change must
    have a clear correction rationale
  - Preserve leading spaces on each word token (whisper convention: " hello", " world")
  - When adding a word at the start, it must NOT have a leading space
    (e.g. "It", not " it")
  - Segments with "context": true are for reference only — do not modify them
  - Do NOT change words you are uncertain about — keep the original

Examples:
  ── homophone fix (same count) ──
  Input:  [{"index": 0, "words": [" there", " model", " is", " good"], "context": false}]
  Output: [{"index": 0, "words": [" their", " model", " is", " good"]}]

  ── proper noun fix (same count) ──
  Input:  [{"index": 1, "words": [" Ministrol", " 3B", " models"], "context": false}]
  Output: [{"index": 1, "words": [" Mistral", " 3B", " models"]}]

  ── missing subject (+1) ──
  Input:  [{"index": 2, "words": [" is", " a", " way", " to"], "context": false},
           {"index": 3, "words": [" solve", " the", " problem"], "context": true}]
  Output: [{"index": 2, "words": ["There", " is", " a", " way", " to"]}]

  ── duplicate removal (-1) ──
  Input:  [{"index": 4, "words": [" A", " A", " speech", " model"], "context": false}]
  Output: [{"index": 4, "words": [" A", " speech", " model"]}]

  ── split compound merge (-2) ──
  Input:  [{"index": 5, "words": [" supercali", " fragilistic", " expialidocious"], "context": false}]
  Output: [{"index": 5, "words": [" supercalifragilisticexpialidocious"]}]

  ── unchanged (already correct) ──
  Input:  [{"index": 6, "words": ["I", " think", " it's", " good"], "context": false}]
  Output: [{"index": 6, "words": ["I", " think", " it's", " good"]}]

Return ONLY a JSON array:
[{"index": <int>, "words": ["<word1>", "<word2>", ...]}, ...]
